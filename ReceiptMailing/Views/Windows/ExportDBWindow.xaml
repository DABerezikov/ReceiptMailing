<Window x:Class="ReceiptMailing.Views.Windows.ExportDBWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        DataContext="{Binding ExportDbViewModel, Source={StaticResource Locator}}"
        Title="{Binding Title}"
        Width="520" SizeToContent="Height"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <DockPanel>

        <!-- Menu -->
        <Menu IsMainMenu="True">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="Файл">
                <MenuItem Header="Выход" Command="{StaticResource CloseWindowCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Status bar -->
        <StatusBar>
            <StatusBarItem Content="{Binding Status}"/>
        </StatusBar>

        <!-- Content -->
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Style="{StaticResource SectionHeaderStyle}"
                               Text="ЭКСПОРТ В EXCEL"/>

                    <!-- Последний сохранённый файл -->
                    <Border Grid.Row="1"
                            BorderBrush="{StaticResource BorderColorBrush}"
                            BorderThickness="1" CornerRadius="5"
                            Background="{StaticResource SurfaceSecondaryBrush}"
                            Padding="8,0">
                        <TextBlock Text="{Binding SavePath, TargetNullValue='Файл не выбран'}"
                                   VerticalAlignment="Center"
                                   Height="32"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"/>
                    </Border>

                    <!-- Прогресс-бар (показывается во время экспорта) -->
                    <StackPanel Grid.Row="3"
                                Visibility="{Binding IsExporting, Converter={StaticResource BoolToVis}}">
                        <ProgressBar Height="6"
                                     IsIndeterminate="True"
                                     Background="{StaticResource BorderColorBrush}"
                                     Foreground="{StaticResource PrimaryBrush}"
                                     BorderThickness="0"/>
                        <TextBlock Text="{Binding Status}"
                                   FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Right"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <Border Grid.Row="4" Height="10"
                            Visibility="{Binding IsExporting, Converter={StaticResource BoolToVis}}"/>

                    <!-- Кнопка -->
                    <Button Grid.Row="5"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Content="Экспортировать в Excel"
                            HorizontalAlignment="Right"
                            Command="{Binding ExportDbCommand}"/>
                </Grid>
            </Border>

        </Grid>
    </DockPanel>
</Window>
