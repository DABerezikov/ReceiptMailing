<Window x:Class="ReceiptMailing.Views.Windows.ImportDBWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        DataContext="{Binding ImportDbViewModel, Source={StaticResource Locator}}"
        Title="{Binding Title}"
        Width="520" SizeToContent="Height"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterOwner">

    <DockPanel>

        <!-- Menu -->
        <Menu IsMainMenu="True">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="Файл">
                <MenuItem Header="Выход" Command="{StaticResource CloseWindowCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Status bar -->
        <StatusBar>
            <StatusBarItem Content="{Binding Status}"/>
        </StatusBar>

        <!-- Content -->
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Style="{StaticResource SectionHeaderStyle}"
                               Text="ФАЙЛ EXCEL С САДОВОДАМИ"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="32"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                BorderBrush="{StaticResource BorderColorBrush}"
                                BorderThickness="1" CornerRadius="5"
                                Background="{StaticResource SurfaceSecondaryBrush}"
                                Padding="8,0">
                            <TextBlock Text="{Binding XlsxFilePath}"
                                       VerticalAlignment="Center"
                                       Height="32"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                        </Border>

                        <Button Grid.Column="2" Style="{StaticResource IconButtonStyle}"
                                Width="32" Height="32"
                                fa5:Awesome.Content="Solid_FileExcel"
                                Foreground="{StaticResource SuccessBrush}"
                                Background="{StaticResource SurfaceSecondaryBrush}"
                                ToolTip="Выбрать файл Excel"
                                Command="{Binding OpenXlsxCommand}"/>
                    </Grid>

                    <Button Grid.Row="3" Style="{StaticResource PrimaryButtonStyle}"
                            Content="Импортировать данные"
                            HorizontalAlignment="Right"
                            Command="{Binding ImportParcelsCommand}"/>
                </Grid>
            </Border>

        </Grid>
    </DockPanel>
</Window>
